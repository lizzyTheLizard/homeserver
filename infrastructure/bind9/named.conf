options {
  directory "/var/cache/bind";
  listen-on { any; };
  allow-query { any; };
  recursion no;
  dnssec-validation no;
};

zone "gutschi.site" {
  type master;
  file "/etc/bind/db.gutschi.site";
};
